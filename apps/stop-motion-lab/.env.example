# Server-only. Never prefix with VITE_ — the browser must not see this key.
# Local: set here. Production + Preview: Cloudflare Pages → Settings →
# Environment variables on the tech-demos project (not GitHub Actions secrets).
OPENAI_API_KEY=

# Optional: force canned stub PNGs even when a key is set (cheap UI recordings).
# STUB_OPENAI=1
